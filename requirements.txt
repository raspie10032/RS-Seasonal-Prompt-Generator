# Only needed for the "Gemma TIPO Prompt -> Tags" node (the Seasonal
# generator node has NO extra dependencies).
#
# llama-cpp-python : in-process GGUF inference. >=0.3.23 is REQUIRED — older
#                    builds cannot load the gemma4 architecture. Native
#                    package; if the default install fails, use the prebuilt
#                    CPU wheel index:
#                    pip install "llama-cpp-python>=0.3.23" \
#                      --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
# tipo-kgen        : used ONLY for its lightweight, pure-Python tag formatter
#                    (kgen.formatter); kgen.executor / kgen.models are never
#                    imported, so no torch/transformers is pulled in at runtime.
# huggingface_hub  : pure-Python; auto-downloads the default GGUF into
#                    ComfyUI/models/gguf when "(auto: download default)" is selected.
llama-cpp-python>=0.3.23
tipo-kgen
huggingface_hub
